<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oerlex.android_assignment2">

    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".alarmClock.AlarmClockActivity" />
        <activity android:name=".countryList.CountryList" />
        <activity android:name=".countryList.CountryInput" />
        <activity android:name=".alarmClock.AlarmSetActivity" />
        <activity android:name=".countryList.MyPreferenceActivity"/>
        <activity android:name=".mp3Player.MP3Player"/>
        <service android:name=".mp3Player.MyService"/>
    </application>

</manifest>